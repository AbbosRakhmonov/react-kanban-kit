import { default as React } from 'react';

interface Props {
    renderColumnAdder: () => React.ReactNode;
}
declare const ColumnAdder: ({ renderColumnAdder }: Props) => import("react/jsx-runtime").JSX.Element;
export default ColumnAdder;
